# The example program, built with agondev. Assemble the library into
# lib/libbytes.a first, as docs/zap-with-agondev.md shows; LIBS links it.
NAME=bytes
LIBS=-lbytes
LDHAS_ARG_PROCESSING = 0
LDHAS_EXIT_HANDLER = 0

include $(shell agondev-config --makefile)
