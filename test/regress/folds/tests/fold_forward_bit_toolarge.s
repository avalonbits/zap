; Refused by both, and refused when the label is settled rather than when the
; line is read, which is the point of the check being in the patcher as well.
    .assume adl=1
    .org $40000
    bit n, a
n:  EQU 8
